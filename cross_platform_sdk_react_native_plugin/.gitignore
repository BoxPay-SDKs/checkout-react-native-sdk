# ==> .gitignore for a React Native Library <==

# Dependency directories
# Never commit installed packages.
node_modules/

# Build output
# This is the compiled JavaScript/TypeScript from your 'src' folder.
lib/

# Android build folders
# These folders are generated by Android Studio/Gradle and can be huge.
android/build/
android/app/build/
android/.gradle/
android/gradle
android/gradlew
android/gradlew.bat

# iOS build folder
# This is generated by Xcode.
ios/build/
ios/Pods/
ios/Framework

# Lockfiles
# For a library, it's often recommended to ignore the lockfile so the consuming
# application can resolve the dependency tree. For an application, you would commit this.
package-lock.json
yarn.lock

# Log files from package managers
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor and OS specific files
# These are specific to a user's machine and should not be shared.
.idea/
.vscode/
*.iml
*.suo
.DS_Store
Thumbs.db

# Environment variables
# Never commit files with secrets or local environment configurations.
.env
.env.*
!.env.example

# Temporary files from npm pack
*.tgz
.github
.yarn
.editorconfig
.gitattributes
.nvmrc
.watchmanconfig
.yarnrc.yml
CODE_OF_CONDUCT.md
CONTRIBUTING.md
LICENSE
.tsconfig.build.json
README.md